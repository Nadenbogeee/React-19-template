import { CardComp } from "@/components/ReactComponents/CardComp";

const HomePage = () => {
  return (
    <main className="flex h-screen w-screen items-center justify-center bg-amber-700">
      <CardComp />
    </main>
  );
};

export default HomePage;
